const grid=document.getElementById('numberGrid');const selected=[];const reserved=new Set([14,27,55,67,82]);const sold=new Set([3,22,38,67,99]);
for(let i=0;i<100;i++){const b=document.createElement('button');b.type='button';b.textContent=String(i).padStart(3,'0');if(sold.has(i))b.classList.add('sold');else if(reserved.has(i))b.classList.add('reserved');b.addEventListener('click',()=>{if(b.classList.contains('sold')||b.classList.contains('reserved'))return;const n=String(i).padStart(3,'0');const p=selected.indexOf(n);if(p>=0){selected.splice(p,1);b.classList.remove('selected')}else if(selected.length<10){selected.push(n);b.classList.add('selected')}update()});grid.appendChild(b)}
function update(){document.getElementById('selectedNumbers').textContent=selected.length?selected.join(', '):'nenhum';document.getElementById('total').textContent=new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(selected.length*10)}
document.getElementById('continueBtn').addEventListener('click',()=>{if(!selected.length){alert('Selecione pelo menos um número.');return}alert('Prévia visual: aqui entraremos com a etapa de pagamento na próxima fase. Números: '+selected.join(', '))});
